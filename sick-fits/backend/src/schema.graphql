# import * from './generated/prisma.graphql'
#above imports things we dont have that are referencing (prisma not graphql)
#helps cut down duplication

type Mutation {
    createItem(title: String, 
    description: String, 
    price: Int, 
    image: String, 
    largeImage: String): Item!    
}

type Query {
    items: [Item]!
}